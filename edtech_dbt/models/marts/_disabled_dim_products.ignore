-- Dimension table: products with clean product identifiers and names

with source as (

    select * from {{ ref('stg_products') }}

),

renamed as (

    select
        "LP ID"       as lp_id,
        product_name,
        provider,
        primary_product,
        product_type,
        sector,
        subcategory
    from source

)

select distinct * from renamed
